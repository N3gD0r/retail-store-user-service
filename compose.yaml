services:
  database:
    image: postgres:latest
    container_name: retail
    restart: always
    environment:
      - POSTGRES_PASSWORD=${DBPASSWORD}
      - POSTGRES_USER=${DBUSER}
      - POSTGRES_DB=${DBNAME}
    expose:
      - 5432
    ports:
      - '5433:5432'
